/** localStorage keys shared by the editor, its autosave and the preview tab. */
export const autosaveKey = (pageId: number) => `rwpb-autosave-${pageId}`;
export const previewKey = (pageId: number) => `rwpb-preview-${pageId}`;
